services:
  api-gateway:
    build: ./services/api-gateway
    ports:
      - "3000:3000"
    depends_on:
      - user-service
      - event-service
      - notification-service
      - payment-service
    environment:
      - USER_SERVICE_URL=http://user-service:4001
      - EVENT_SERVICE_URL=http://event-service:4003
      - NOTIFICATION_SERVICE_URL=http://notification-service:4004
      - PAYMENT_SERVICE_URL=http://payment-service:4005
    env_file:
      - .env
    volumes:
      - ./services/api-gateway:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 3000 --reload

  user-service:
    build: ./services/user-service
    ports:
      - "4001:4001"
    depends_on:
      - mongo
    environment:
      - MONGO_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongo:27017
      - MONGO_DB=users_db
    env_file:
      - .env
    volumes:
      - ./services/user-service:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 4001 --reload

  event-service:
    build: ./services/event-service
    ports:
      - "4003:4003"
    depends_on:
      - mongo
    environment:
      - MONGO_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongo:27017
      - MONGO_DB=users_db
    env_file:
      - .env
    volumes:
      - ./services/event-service:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 4003 --reload

  notification-service:
    build: ./services/notification-service
    ports:
      - "4004:4004"
    depends_on:
      - mongo
      - user-service
      - event-service
    environment:
      - MONGO_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongo:27017
      - MONGO_DB=users_db
      - FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json
      - EVENTOS_URL=http://event-service:4003
      - USUARIOS_URL=http://user-service:4001
    env_file:
      - .env
    volumes:
      - ./services/notification-service:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 4004 --reload

  payment-service:
    build: ./services/payment-service
    ports:
      - "4005:4005"
    depends_on:
      - mongo
      - user-service
      - event-service
    environment:
      - MONGO_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongo:27017
      - MONGO_DB=users_db
      - USER_SERVICE_URL=http://user-service:4001
      - EVENT_SERVICE_URL=http://event-service:4003
      - EPAYCO_P_CUST_ID=${EPAYCO_P_CUST_ID}
      - EPAYCO_P_KEY=${EPAYCO_P_KEY}
      - EPAYCO_PUBLIC_KEY=${EPAYCO_PUBLIC_KEY}
      - EPAYCO_TEST=${EPAYCO_TEST}
      - PAYMENT_CALLBACK_URL=${PAYMENT_CALLBACK_URL}
      - PAYMENT_RESPONSE_URL=${PAYMENT_RESPONSE_URL}
    env_file:
      - .env
    volumes:
      - ./services/payment-service:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 4005 --reload

  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
